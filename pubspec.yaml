name: moodlog0
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # 기본 Flutter 아이콘
  cupertino_icons: ^1.0.8

  # ✅ Firebase 관련
  firebase_core: ^3.1.1
  firebase_auth: ^5.1.1

  # ✅ Google 로그인
  google_sign_in: ^6.2.1

  # ✅ 상태관리
  provider: ^6.1.2

  # ✅ (선택) 나중에 피드 Firestore로 옮길 때 필요
  cloud_firestore: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

